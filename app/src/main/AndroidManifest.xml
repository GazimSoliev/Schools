<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.gazim.school">

    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name="com.gazim.school.InstitutionApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_school"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_school_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.School">
        <activity
            android:name="com.gazim.school.presentation.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>